@startuml
start

note left
Authorization
end note
repeat
repeat
:The user presses the login button]
backward :    Output message:\n"Fill in the input fields";
repeat while (Input fields are not empty?) is (No) not (Yes)
: Sending a data packet to the server;
backward :               Output message:\nUsername or password is incorrect,\n                Please try again;
repeat while (Is there a user with such data to log in to the database?) is (No) not (Yes)
:Go to the main menu;

end
@enduml